@model Nop.Plugin.Widgets.HomePageNewProducts.Models.ConfigurationModel
@using Nop.Web.Framework

@{
    Layout = "";
}

@Html.Action("StoreScopeConfiguration", "Setting", new { area = "Admin" })
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <table class="adminContent">
        <tr>
            <td class="adminTitle">
                @Html.NopLabelFor(model => model.WidgetZone):
            </td>
            <td class="adminData">
                @Html.EnumDropDownListFor(model => model.WidgetZone)
                @Html.ValidationMessageFor(model => model.WidgetZone)
            </td>
        </tr>
        <tr>
            <td class="adminTitle">
                @Html.NopLabelFor(model => model.NumberOfProducts):
            </td>
            <td class="adminData">
                @Html.EditorFor(model => model.NumberOfProducts)
                @Html.ValidationMessageFor(model => model.NumberOfProducts)
            </td>
        </tr>
    </table>
        <input type="submit" name="save" class="k-button" value="@T("Admin.Common.Save")" />
}
